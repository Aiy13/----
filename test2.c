main() {
    // 合法表达式
    x/y;                  // 二元运算
    (3+5)*56-90;         // 复杂算术表达式
    2.5 + 3.7;           // 浮点数运算
    ((2 + 3) * (4 - 1)) / (5 + 2);  // 多层括号
    3e-2 + 0.5;          // 科学计数法
    2e3 - 9;             // 科学计数法
    x = 1;               // 赋值语句
    y = x + 2;           // 赋值结合算术表达式

    // 错误情况
    a*;                
    +b;                
    (2 + 3 * 4;        
    x = ;              
    = 1;
    a = (c + 6)*7);    //缺少左括号    
    // 控制结构
    if (x > 0) {         // 简单if语句
        x = x - 1;
    }
    if (x > 0 && y < 10) {  // 复杂条件表达式
        y = y + 1;
    } else {
        y = 0;
    }
    while (x <= 5) {     // while循环
        x = x + 1;
    }
    if (x == 0) {        // 嵌套if
        if (y != 0) {
            z = x + y;
        }
    }
}